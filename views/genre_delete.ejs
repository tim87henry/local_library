<!DOCTYPE html>
<html>
  <%- include('head'); -%>
  <body>
    <div class="container-fluid">
        <div class="row">
            <%- include('layout'); -%>
            <div class="col-sm-10">
              <h1><%= title %>: <%= genre.name %></h1>
              <% if (books.length) { %>
                  <p><strong>Delete the following books before attempting to delete this genre.</strong></p>
                  <div style="margin-left:20px;margin-top:20px">
                    <h4>Books</h4>
                    <dl>
                        <% books.forEach(book => { %>
                            <dt>
                                <a href=<%= book.url %> ><%= book._id %></a>
                            </dt>
                            <dd>
                                <p><strong><Title></Title>: </strong><%= book.title %></p>
                                <p><strong><Title></Title>: </strong><%= book.author.name %></p>
                            </dd>
                        <% }) %>
                    </dl>
                  </div>
              <% } else { %>
                  <p>Do you really want to delete this Genre?</p>
                  <form method="POST" action="">
                      <div class="form-group">
                          <input id="genreid" class="form-control" type="hidden" name="genreid" required="true" value=<%= genre._id %> >
                      </div>
                      <button class="btn btn-primary" type="submit">Delete</button>
                  </form>
              <% } %>
            </div>
        </div>
    </div>
  </body>
</html>